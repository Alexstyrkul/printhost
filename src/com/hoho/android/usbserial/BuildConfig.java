package com.hoho.android.usbserial;

/** Stand-in for the Gradle-generated BuildConfig - this project has no Gradle build, so the
 *  library's debug-log gate is just hardcoded off here. */
public final class BuildConfig {
    public static final boolean DEBUG = false;
    private BuildConfig() {}
}
